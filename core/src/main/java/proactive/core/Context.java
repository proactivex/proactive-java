package proactive.core;

/**
 * A context object, generally provided by a {@link ProactiveDriver}, which indicates the type of value
 * being handled.
 *
 * @param <T> The value type.
 */
public interface Context<T> {
}
